[project]
name = "gravitational-transform"
version = "0.1.0"
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project.scripts]
gravitational-transform = "gt.cli:run"
